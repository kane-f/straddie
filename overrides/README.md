# Theme overrides

This directory is wired up as `theme.custom_dir` in `mkdocs.yml`. It's empty
for now — Material for MkDocs falls back to its defaults when there's
nothing here to override.

Use it later for things like a customised homepage partial or a footer
tweak. Don't put content pages here; those belong under `docs/`.
