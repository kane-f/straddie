# Working agreements

- Never commit directly to `main`. Always create a feature branch for any change, and open a pull request for review instead of pushing straight to `main`.
- Never include passwords, wifi credentials, API keys, or other secrets in any file added to this repo. This repo is public.
